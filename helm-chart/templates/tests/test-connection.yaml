apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hariyalibackend.fullname" . }}-test-connection"
  labels:
    {{- include "hariyalibackend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hariyalibackend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
